#include <bits/stdc++.h> 
using namespace std; 
void count_digits(int n) 
{ 
    string num = to_string(n); 
      cout << num.size() << endl; 
} 
int main() 
{ 
   int n; 
  cin>>n;
    count_digits(n); 
    return 0; 
} 